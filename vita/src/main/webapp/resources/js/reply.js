
$(function () {
    var replyPageNo = 0;
    
	$('버튼아이디').on('click', function() {
        // 승현 추가
        replyPageNo = 0;
        var sendData = {
        		"파라미터이름" : 값,
        		"" : 값
        }
        replyService.regist(sendData, function(result) {
        	//reuslt
        	// li 추가 append
        });
        
    });
	
});